import { connect } from 'react-redux'

import AddInterviewReview from 'components/AddInterviewReview'

const mapDispatchToProps = {
}

const mapStateToProps = (state) => ({
})

export default connect(mapStateToProps, mapDispatchToProps)(AddInterviewReview)
