import { connect } from 'react-redux'

import AddSchoolReview from 'components/AddSchoolReview'

const mapDispatchToProps = {
}

const mapStateToProps = (state) => ({
})

export default connect(mapStateToProps, mapDispatchToProps)(AddSchoolReview)
